# Prompt to paste into Claude Code

You are working inside the `competitive-intelligence-assistant` repository. Read `CLAUDE.md` and `README.md` first, then inspect and run the project.

Your objective is to make this a polished, reliable two-minute portfolio demo for a candidate pursuing a Salesforce GTM Product Marketing Manager role.

Complete the following work without expanding the product beyond its narrow promise:

1. Run the tests and fix any failures.
2. Run the Streamlit app and inspect the complete demo flow.
3. Improve the UI so the story is immediately understandable: competitor → detected changes → PMM brief.
4. Keep the output structure grounded in Observation → Inference → Recommended Action, with evidence and confidence.
5. Harden crawling, snapshot comparison, timeout handling, and Claude CLI error messages.
6. Ensure demo mode works instantly and live mode uses the authenticated `claude -p` command without an API key.
7. Improve the README only where it increases recruiter comprehension or setup reliability.
8. Do not add authentication, a database server, multiple agents, CRM integrations, or a large dashboard.
9. Do not commit secrets.
10. After making changes, run tests again and summarize exactly what you changed, any remaining limitations, and the commands I should run to launch the demo.

Treat simplicity, credibility, and demo clarity as the highest priorities. Make edits directly in the repository.
